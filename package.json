{
  "name": "@full-aigc-plugins/content-factory-plugin",
  "version": "0.1.0-dev",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "test:supply-chain": "python -m unittest discover -s tests/supply-chain -p 'test_*.py' -v",
    "check:skills": "python scripts/vendor/skill_vendor.py check"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
