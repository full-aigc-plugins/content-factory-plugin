{
  "name": "content-factory-plugin",
  "version": "0.0.0-development",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "test:cf001": "node --test tests/contract/manifests.test.mjs"
  }
}
