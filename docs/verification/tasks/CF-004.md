# CF-004 — WeChat draft permission/readback probe

Status: ADAPTER_IMPLEMENTED_LIVE_NOT_RUN

## Automated evidence

- RED test commit: `48998a23edf63148814d9f8f185c199eb07615eb`
- Test typing cleanup: `6d26fb5175bd54b8f9ee6efea1386205026d5b6d`
- Clean RED workflow: `35546522953`, build failed only because `src/external/wechat-draft.ts` was absent.
- GREEN evidence is the CI run attached to the implementation commit.

## Implemented contract

- missing access token blocks mutation before transport invocation;
- create uses the WeChat draft/add contract and records the returned remote media id;
- successful create is `submitted_unverified`, never automatically `verified`;
- a transport exception during create becomes `unknown` and is not blindly retried;
- readback uses the draft/get contract and preserves raw remote content;
- non-2xx and WeChat application errors are `failed`;
- missing `media_id` in a nominally successful create response is `failed`.

## Live gate

A real authorized Official Account create/read/update probe is still **NOT_RUN**. Public broadcast is intentionally outside this task. Final acceptance requires an authorized account and stored evidence for permissions, returned identifiers, readback, and unknown-write reconciliation.
