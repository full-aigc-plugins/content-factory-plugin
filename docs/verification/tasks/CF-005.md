# CF-005 — Vendor formatting parity baseline

Status: BLOCKED_UPSTREAM_IMMUTABLE_SOURCE

## Finding

The current Content Factory plan selects `baoyu-format-markdown` and
`baoyu-markdown-to-html`, but the source identity requires reconciliation
before these skills can enter `skills.lock.json`.

The historical Image Factory lock references
`https://github.com/partme-ai/baoyu-skills.git@v2.5.2`, while the repository
currently redirects to the moved `full-aigc-skills/baoyu-skills` identity.
The moved repository's directly queried current tags did not expose that
historical release in a form that the GitHub connector could read as the
current skill snapshot.

## Ruling

Do not fabricate a tag, digest, license record, or vendor snapshot.
CF-005 remains blocked until an immutable readable release is published or
a controlled reviewed snapshot is released under the current skill
organization.

This does not block CF-006 or the Runtime Kernel tasks.
