# CF-003 — Zhuque live contract probe

Status: ADAPTER_IMPLEMENTED_LIVE_NOT_RUN

## Automated evidence

- RED test commit: `b55564c1b586a33e72ae3940f6e6f9a388745658`
- Test typing cleanup: `74deec1de0ab1f3da0906a1d947f5b78657def55`
- Clean RED workflow: `35546392919`, build failed only because `src/external/zhuque.ts` was absent.
- GREEN evidence is the CI run attached to the implementation commit.

## Implemented

- no network call without an explicit token;
- exact UTF-8 byte count and SHA-256 bound to submitted text;
- bearer authorization and JSON body contract;
- raw response retention;
- HTTP status retention;
- non-2xx and transport errors remain failures;
- no invented normalization of score, ratio, confidence, or segment semantics.

## Live gate

A real authorized Zhuque request is still **NOT_RUN**. The task is not eligible for final V1 completion until an authorized account validates the provider's current request/response fields, Unicode offsets, quota behavior, and raw evidence format.
