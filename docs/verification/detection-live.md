# AI 内容检测平台真实验证

Live status: `NOT_RUN`

本仓库当前只完成了本地契约、合成响应、Unicode 定位、审阅政策、失效规则、预算重试与报告渲染验证。按照用户授权，本轮跳过真实外部账号调用。

当前没有以下真实证据：

- 人工稿的真实 API 响应与原始响应哈希；
- 原始 AI 稿的真实 API 响应与原始响应哈希；
- 编辑稿的真实 API 响应与原始响应哈希；
- 网页结果与 API 结果的一致性对照。

因此，本页和本地 fixture 不能作为生产准入证据，也不能被解释为检测通过、作者身份判断或插件质量证明。若以后补充真实验证，必须保留精确文本哈希、请求身份、不可变原始响应、策略版本和人工审阅记录。
